#include<iostream>
#include<vector>
#include<string>
#include<cctype>

using namespace std;

int main()
{
    vector<string> svec;
    string word;

    while(cin >> word)
    {
        svec.push_back(word);
    }

    int i  = 0;
    for(vector<string>::size_type s = 0; s != svec.size(); s++)


    return 0;
}
